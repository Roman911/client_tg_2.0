import type { NextPage } from 'next'
import { MainLayouts } from "../layouts"

const Home: NextPage = () => {
  return <div>
    <MainLayouts >
      Index
    </MainLayouts>
  </div>
}

export default Home